---
interface Props {
	theme?: "DARK" | "MID" | "THEME";
}

const { theme } = Astro.props;
---

<section class:list={['strip', { dark: theme === 'DARK', mid: theme === 'MID' }, 'full-bleed']}>
	<slot />
</section>
<style>
	.strip {
		background: transparent;
	}

	.dark {
		background: var(--palette-dark);
		color: var(--palette-light);
	}

	.mid {
		background: var(--palette-mid);
		color: var(--palette-light);
	}
</style>
